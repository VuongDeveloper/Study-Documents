package com.ts.gateway.config;

import org.springframework.context.annotation.Configuration;

/**
 * Route definitions are managed externally via the Config Server (api-gateway.yml).
 * This class is a placeholder for any future programmatic route customisation.
 */
@Configuration
public class RouteConfig {
    // Routes are defined in api-gateway.yml served by config-server
}
